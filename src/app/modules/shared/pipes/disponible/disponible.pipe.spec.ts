import { DisponiblePipe } from './disponible.pipe';

describe('DisponiblePipe', () => {
  it('create an instance', () => {
    const pipe = new DisponiblePipe();
    expect(pipe).toBeTruthy();
  });
});
